{
  "name": "remote-data",
  "version": "1.0.0",
  "description": "Data model that represents the four states of an HTTP request",
  "main": "src/RemoteData.js",
  "repository": "https://github.com/f4b13n/flow-remote-data.git",
  "author": "fb3rn4rd",
  "license": "MIT",
  "scripts": {
    "flow": "flow",
    "test": "yarn flow && tape -r babel-register test/*.spec.js | tap-spec"
  },
  "dependencies": {
    "ramda": "^0.25.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-preset-flow": "^6.23.0",
    "babel-register": "^6.26.0",
    "flow-bin": "^0.69.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.8.0"
  }
}
